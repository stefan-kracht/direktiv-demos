direktiv_api: workflow/v1
description: Reads the first person from the namespace variable
states:
  - id: get-value
    type: getter
    variables:
      - key: persons
        scope: namespace
    transform:
      data: jq(.var.persons.data.[0])
